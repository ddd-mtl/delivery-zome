use hdk::prelude::*;

/// Zome Function
#[hdk_extern]
pub fn pull_inbox(_:()) -> ExternResult<Vec<HeaderHash>> {
   /// Get all inbox links

   /// Convert Each Item


   /// Process all Descriptions

   /// Process all DeliveryNotifications

   /// Process all ReceptionConfirmations

   ///Process all Parcels
   /// Get list of each incomplete Parcel or Description
   /// Download missing parcels

   Ok(Vec::new())
}