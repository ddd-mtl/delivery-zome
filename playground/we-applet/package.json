{
  "name": "we-applet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .ts .",
    "tsc": "rm -rf out-tsc && tsc",
    "build": "rm -rf dist && vite build",
    "devtest": "vite dev --clearScreen false",
    "start:ui": "vite dev --port $UI_PORT --clearScreen false",
    "start": "UI_PORT=$(port) HC_APP_PORT=$(port) HC_ADMIN_PORT=$(port) HAPP_BUILD_MODE='Debug' concurrently \"npm run start:happ:mem\" \"sleep 5 && npm run start:ui\"",
    "start:happ:mem": "RUST_LOG=warn WASM_LOG=warn echo \"pass\" | hc s --piped -f=$HC_ADMIN_PORT generate ../../artifacts/secret.happ --run=$HC_APP_PORT -a hSecret network mem",
    "clean:hc": "rm -rf .hc*"
  },
  "devDependencies": {
    "vite": "=4.5.2",
    "vite-plugin-checker": "=0.5.3",
    "vite-plugin-dts": "=4.5.3",
    "vite-plugin-top-level-await": "=1.5.0",
    "bestzip": "2.2.0",
    "concurrently": "5.3.0",
    "deepmerge": "4.2.2",
    "new-port-cli": "1.0.0",
    "tslib": "2.8.0",
    "typescript": "~5.9.0"
  },
  "dependencies": {
    "secret-playground": "file:../webapp",
    "@holochain-open-dev/profiles": "^0.701.0",
    "@theweave/api": "0.7.0-dev.3",
    "@ddd-qc/we-utils": "^0.36.0",
    "@mdi/js": "^7.2.0"
  }
}
